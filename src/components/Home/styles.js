import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  spinnerContainer: {
    display: 'flex',
    justifyContent: 'center',
  },
  countryPicker: {
    textAlign: 'center',
    marginBottom: '30px',
  },

  topNews: {
    textAlign: 'center',
    marginBottom: '15px',
  },
})

export { useStyles }
