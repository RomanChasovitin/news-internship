import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  spinnerContainer: {
    display: 'flex',
    justifyContent: 'center',
    padding: '30px 0',
  },
  countryPicker: {
    textAlign: 'center',
    marginBottom: '30px',
  },

  title: {
    textAlign: 'center',
    marginBottom: '15px',
  },
})

export { useStyles }
