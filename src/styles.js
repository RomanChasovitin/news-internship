import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  root: {
    display: 'flex',
  },
  content: {
    width: '100%',
    padding: '10px',
    marginTop: '65px',
  },
}))

export { useStyles }
